# 当前任务

当前仓库只保留与自然语料论文主线直接相关的任务代码。

## S1 · 三轴 scaling

当前唯一活动任务目录是 tasks/s1_scaling_three_axis/，覆盖 epoch length、exact frequency 和 clean table size 三条自然语料轴。

旧的 ngram5_freq_gap/ 受控数据 trainer 以及 L1–L6 toy / synthetic 任务已于 2026-09-13 退役并从当前工作树移除。历史提交仍可用 git log --all -- path 追溯；它们不再是活动实验入口，也不应作为新论文证据。
